<div class="maindiv">
	<h1>Alle absolvierten Trainings</h1>

	<table id="realised_trainings">
  		<thead>
  			<th><%= link_to "Name"%></th>
  			<th><%= link_to "Date"%></th>
  			<th><%= link_to "Duration"%></th>
  			<th><%= link_to "Description"%></th>
  			 <th>Löschen</th>
		</thead>
		
  		<tbody>
  		<% @realised_trainings.each do |realised_training|%>
		<tr>
			<td>
				<%= link_to realised_training_path(realised_training.id) do  %>
				<%if realised_training.name%>
     			<%= realised_training.name %>
     			<%else%> Neu erstelltes Training
     			<%end%>
     			<%end%>
     		</td>
     		<td>
				<%= link_to realised_training_path(realised_training.id) do  %>
     			<%= realised_training.date%>
     			<%end%>
     		</td>
			<td>
				<%= link_to realised_training_path(realised_training.id) do %>
				<%= realised_training.duration%>
     			<%end%>
			</td>
			<td>
				<%= realised_training.description%>				
			</td>
			<td>
				<%= link_to "delete", realised_training_path(realised_training.id), :method => 'delete'  %>		
			</td>
			
		</tr>
		<%end%>
		</tbody>
	</table>
<%= link_to "Neu", '#', :method => 'post'%>
</div>

